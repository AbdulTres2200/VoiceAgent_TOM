{
  "name": "check_service_area",
  "description": "Verify if an address is within the service area and check H+ membership status. Call this IMMEDIATELY after collecting the customer's full address. The response includes verified is_hplus_member status - use this value for check_business_hours, not what customer claimed.",
  "parameters": {
    "type": "object",
    "properties": {
      "address": {
        "type": "string",
        "description": "Customer's full address including street, city, state, and zip code. Example: '123 Main Street, Painesville, OH 44077'"
      },
      "phone": {
        "type": "string",
        "description": "Customer's phone number for membership lookup"
      }
    },
    "required": ["address", "phone"]
  }
}
